db:
  url: "$DATABASE_URL"

models:
  - "actiondash.models"

redis:
  url: "$REDIS_URL"
  prefix: "actiondash"

notifications:
  backend: "skrift.lib.notification_backends:RedisBackend"
  webhook_secret: "$NOTIFICATION_WEBHOOK_SECRET"

auth:
  redirect_base_url: "$AUTH_REDIRECT_BASE_URL"
  allowed_redirect_domains:
    - "localhost"
  providers:
    dummy: {}

session:
  max_age: 604800

security_headers:
  enabled: false

rate_limit:
  enabled: false

controllers:
  - skrift.controllers.auth:AuthController
  - skrift.admin.controller:AdminController
  - actiondash.controllers:DashboardController
  - actiondash.controllers:RepoController
  - actiondash.controllers:SettingsController
  - actiondash.controllers:HealthController
  - actiondash.controllers:WebhookController
  - actiondash.worker_admin:WorkerAdminController
