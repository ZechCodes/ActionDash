apiVersion: v1
kind: ConfigMap
metadata:
  name: actiondash-config
  labels:
    app: actiondash
data:
  AUTH_REDIRECT_BASE_URL: "https://actiondash.example.com"  # CHANGEME: your domain
  SESSION_COOKIE_DOMAIN: ".example.com"  # CHANGEME: your domain
  NOTIFICATION_WEBHOOK_URL: "http://actiondash:8080/notifications/webhook/"
