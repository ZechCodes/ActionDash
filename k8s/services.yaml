apiVersion: v1
kind: Service
metadata:
  name: actiondash
  namespace: zechcodes
  labels:
    app: actiondash
spec:
  type: ClusterIP
  selector:
    app: actiondash
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
