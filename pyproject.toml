[project]
name = "actiondash"
version = "0.1.0"
description = "Realtime GitHub Actions dashboard built with Skrift"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "asyncpg",
    "redis>=5.0",
    "skrift>=0.1.0a28",
]
